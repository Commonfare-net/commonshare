
wks_active = weeks_active(N,start,CurrentDate)
edgeweights = []
    for (N,v) in edges
        D = 0.75
        TotalWeight = 0
        FirstDate = (N,v).interactions[0].date
        influence = weeks_active(v,first_interaction,end)/week_diff(FirstDate,CurrentDate)
        for i in (N,v).interactions
            AgeFraction = e^(-day_diff(i.date,CurrentDate)/50)
            TotalWeight += i.weight*Age_Factor*DC
            DC *= 0.75
            
        overallweight = (e^influence -1) * root(wks_active) + 0.1),1)
return sum(edgeweights)        
        